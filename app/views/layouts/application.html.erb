<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for :page_title %> - Bien Reviews - The best restaurants in the world</title>
    <%= favicon_link_tag asset_path('bien-page-icon.svg') %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <header>
      <h1>

        <%= link_to root_path  do %>
          <%= image_tag "bien-logo.svg" %>
        <% end %>

      </h1>

      <nav>

        <% if is_logged_in? %>
          Hi, <%= @current_user.username %>
          <%= link_to "Add a review", new_review_path %>
          <%= link_to "Log out", session_path, method: :delete %>
        <% else %>
          <%= link_to "Sign up", new_user_path %>
          <%= link_to "Log in", new_session_path %>
        <% end %>




      </nav>
    </header>

    <main>
    <%= yield %>
    </main>

    <footer>
      Copyright 2020 Bien Reviews
    </footer>
  </body>
</html>
